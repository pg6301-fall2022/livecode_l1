import * as React from "react";
import * as ReactDOM from "react-dom/client";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
    <h1>
        Lesson 2 - Movie database,
        {document.getElementById("app")}
    </h1>
);